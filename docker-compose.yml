services:
  bot:
    build: .
    environment:
      BOT_TOKEN: ${BOT_TOKEN}
      BOT_NAME: ${BOT_NAME}
    restart: unless-stopped

    # Long polling: входящий порт не нужен.
    # Если позже добавите webhook/actuator — тогда можно раскомментировать ports.
    # ports:
    #   - "8080:8080"